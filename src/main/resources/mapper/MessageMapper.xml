<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.twilight.pointquestbackend.mapper.MessageMapper">
    <sql id="tableName">
        message
    </sql>

    <sql id="baseColumn">
        id,title,content,sender_id,receiver_id,is_read,created_at,read_at
    </sql>

</mapper>